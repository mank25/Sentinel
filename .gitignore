.venv/
__pycache__/
*.pyc
.env
.DS_Store
data/*.db
.sentinel-mcp-token
*.egg-info/

# Frontend
ui/web/node_modules/
# ui/web/dist is deliberately NOT ignored: it is committed so the console
# runs with Python alone. npm is needed to change the frontend, not to run it.
